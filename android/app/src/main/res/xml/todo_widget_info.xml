<?xml version="1.0" encoding="utf-8"?>
<!-- Widget metadata: resizable both ways, defaults to 4x2 cells.
     updatePeriodMillis=0 — updates are pushed (Dart on every DB change +
     the midnight task), never polled. previewLayout (API 31+) shows the real
     layout in the widget picker; previewImage is the pre-31 fallback. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:minResizeWidth="140dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/todo_widget"
    android:previewLayout="@layout/todo_widget"
    android:previewImage="@mipmap/ic_launcher"
    android:description="@string/widget_description" />
