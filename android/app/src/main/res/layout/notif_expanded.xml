<?xml version="1.0" encoding="utf-8"?>
<!-- Expanded persistent notification. notif_container is filled at runtime
     with notif_item rows via RemoteViews.addView — a RemoteViewsService
     adapter cannot be used inside a notification (app-widget hosts only),
     and the expanded view is height-capped anyway. See
     TodoForegroundService.buildNotification. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="4dp"
    android:paddingBottom="6dp">

    <TextView
        android:id="@+id/notif_expanded_title"
        style="@style/TextAppearance.Compat.Notification.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="4dp"
        android:maxLines="1" />

    <LinearLayout
        android:id="@+id/notif_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <TextView
        android:id="@+id/notif_empty"
        style="@style/TextAppearance.Compat.Notification.Info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/notif_all_done_long"
        android:visibility="gone" />

    <TextView
        android:id="@+id/notif_more"
        style="@style/TextAppearance.Compat.Notification.Info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="2dp"
        android:visibility="gone" />
</LinearLayout>
